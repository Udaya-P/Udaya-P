No GitHub Actions are included in v1 by design.

Automation such as contribution snakes, recent activity feeds, or scheduled SVG regeneration should be added only if explicitly requested, because each workflow has maintenance, privacy, or noise tradeoffs.
